namespace TheCalculator.Core.Options.Features;

/// <summary>
///     Provides feature flag names.
/// </summary>
public static class FeatureFlags
{
    /// <summary>
    ///     A feature flag used to determine if cache should be enabled.
    /// </summary>
    public const string UseCache = nameof(UseCache);
}
